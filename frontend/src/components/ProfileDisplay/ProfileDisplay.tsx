/**
 * This component will be shared between both MyProfile and ViewProfile pages
 * It will display user's profile information such as avatar, username, bio, etc.
 * It will take props to determine whether it's displaying the logged-in user's profile or another user's profile
 * */

const ProfileDisplay = () => {
  return <div>ProfileDisplay</div>;
};
export default ProfileDisplay;
